#include<iostream>
using namespace std;
using std::cin;
using std::cout;
int swap(int *a , int *b){//it will swap the elements
		int temp=*a;
		*a=*b;
		*b=temp;	
}
int union_intersection(int arr[],int n){
	while(i!=n-1 || j!=m-1){
		
	}
int main(){
	cout<<"Enter the no. of values in the arry: ";
	int n,m;
	cin>>n>>m;
	int a[n],b[m];
	for(int i=0;i<n;i++)// input for the array 1
	cin>>a[i];
	for(int i=0;i<n;i++)// input for the array 2
	cin>>b[i];
	bubble(a,b,n,m);
}
